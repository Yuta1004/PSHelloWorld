{
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "purescript": "^0.15.15",
    "purescript-language-server": "^0.18.4",
    "spago": "^0.21.0"
  },
  "scripts": {
    "setup": "run-s setup:*",
    "setup:install": "npm install",
    "setup:deps": "npx spago build --deps-only"
  }
}
